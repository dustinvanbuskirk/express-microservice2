version: '1.0'
kind: pipeline
metadata:
  name: default/spec-1
  description: "Pipeline spec-1"
  deprecate: {}
  project: default
spec:
  contexts: []
  variables: []
  steps:
    test_cli_command:
      image: codefresh/cli:latest
      commands:
        - codefresh create project test-cli-project-1
  stages: []
